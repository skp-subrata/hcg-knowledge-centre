[pytest]
testpaths = tests
addopts = -q
markers =
    slow: long-running tests (large uploads, seed timing)
    qa(id): links a test to a QA finding id in docs/QA_FINDINGS.md
    mutating: destructive operations, always run on a private DB copy
